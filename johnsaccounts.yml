spring:
  profiles.active: local
  application:
     name: johnsaccounts
  jpa:
     hibernate:
        ddl-auto: update

---
spring:
  profiles: local
server:
  port: 8082
eureka:
  instance:
    nonSecurePort: ${server.port}
